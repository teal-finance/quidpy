const quidServerUrl = "http://localhost:8090";
const serverUrl = "http://127.0.0.1:5000";

const testUser = {
  name: "testuser",
  pwd: "testuserpwd"
};

const testNs = "testns";

export { quidServerUrl, serverUrl, testUser, testNs }